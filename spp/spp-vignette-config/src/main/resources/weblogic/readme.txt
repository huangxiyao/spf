This directory contains the WebLogic configuration copied from the corresponding environment. 

The files here are not used as a configuration source, but are rather used to track the configuration
changes performed through the console, such as envolving over time Garbage Collector settings.
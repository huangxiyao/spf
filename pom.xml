<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>com.hp.it.spf.portal</groupId>
		<artifactId>spf-portal-parent</artifactId>
		<version>2.1.1-SNAPSHOT</version>
		<!-- 
			relativePath to parent is only needed in the aggregator POM to allow build all modules
			including parent, even though the parent's snapshot has not been built yet in the CI
			and is not available in the repository.
		-->
		<relativePath>spf-portal-parent/pom.xml</relativePath>
	</parent>

	<groupId>com.hp.it.spf.portal</groupId>
	<artifactId>portal</artifactId>
	<packaging>pom</packaging>
	<name>SPF portal aggregator POM</name>

	<scm>
		<connection>scm:svn:https://code1.corp.hp.com/svn/spf/source/trunk/portal</connection>
		<developerConnection>scm:svn:https://code1.corp.hp.com/svn/spf/source/trunk/portal</developerConnection>
		<url>https://code1.corp.hp.com/svn/spf/source/trunk/portal</url>
	</scm>
	
	<modules>
		<module>spf-maven-car-plugin</module>
		<module>spf-portal</module>
		<module>spf-portal-config</module>
		<module>spf-portal-filters</module>
		<module>spf-portal-parent</module>
		<module>spf-portal-session</module>
		<module>spf-portal-tools</module>
		<module>spf-portal-utilities</module>
		<module>spf-portal-wsrp</module>
	</modules>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-release-plugin</artifactId>
				<version>2.0-beta-9</version>
				<configuration>
					<autoVersionSubmodules>true</autoVersionSubmodules>
					<preparationGoals>clean install</preparationGoals>
				</configuration>
			</plugin>
		</plugins>
	</build>
</project>
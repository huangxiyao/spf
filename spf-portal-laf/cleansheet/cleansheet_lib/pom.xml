<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
    	<groupId>com.hp.frameworks</groupId>
    	<artifactId>spf-hpweb-laf</artifactId>
    	<version>2.2.1-SNAPSHOT</version>
	</parent>

	<artifactId>cleansheet-lib</artifactId>
	<packaging>car</packaging>
	<name>Cleansheet Class Library VAP Component</name>
	
	<dependencies>
		<dependency>
			<groupId>com.hp.it.spf.xa</groupId>
			<artifactId>spf-portal-utilities</artifactId>
			<version>2.2.0</version>
			<scope>provided</scope>
			<exclusions>
        		<exclusion>
          			<groupId>xml-apis</groupId>
          			<artifactId>xml-apis</artifactId>
        		</exclusion>
        		<exclusion>
          			<groupId>xerces</groupId>
          			<artifactId>xercesImpl</artifactId>
        		</exclusion>
        		
      		</exclusions> 
		</dependency>
		<dependency>
			<groupId>com.hp.frameworks</groupId>
			<artifactId>cleansheet-core</artifactId>
			<version>${cleansheet-core-version}</version>
			
<!-- 
		For WL recompile defect workaround, don't package up the real 
		cleansheet-core.jar. Instead the content of cleansheet-core.jar is 
		re-bundled into the car file itself using the plugins in the build section.
		
-->
			<scope>provided</scope>
		</dependency>

		<dependency>
			<groupId>com.hp.frameworks</groupId>
			<artifactId>cleansheet-resources</artifactId>
			<version>${cleansheet-resources-version}</version>
		</dependency>

		<dependency>
			<groupId>oro</groupId>
			<artifactId>oro</artifactId>
			<version>2.0.8</version>
			<scope>provided</scope>
		</dependency>	
		
		<dependency>
			<groupId>taglibs</groupId>
			<artifactId>standard</artifactId>
			<version>1.1.2</version>
			<scope>provided</scope>
		</dependency>

		<dependency>
			<groupId>javax.servlet</groupId>
			<artifactId>jstl</artifactId>
			<version>1.1.2</version>
			<scope>provided</scope>
		</dependency>
		
		<dependency>
			<groupId>javax.servlet</groupId>
			<artifactId>servlet-api</artifactId>
			<version>2.4</version>
			<scope>provided</scope>
		</dependency>
		
		<dependency>
			<groupId>javax.servlet</groupId>
			<artifactId>jsp-api</artifactId>
			<version>2.0</version>
			<scope>provided</scope>
		</dependency>
		
		<dependency>
			<groupId>vignette</groupId>
			<artifactId>vgn-portal-core</artifactId>
			<version>7.4.2</version>
			<scope>provided</scope>
		</dependency>
		
		<dependency>
			<groupId>vignette</groupId>
			<artifactId>vgn-portal-linking</artifactId>
			<version>7.4.2</version>
			<scope>provided</scope>
		</dependency>
		
		<dependency>
			<groupId>vignette</groupId>
			<artifactId>vgn-portal-base</artifactId>
			<version>7.4.2</version>
			<scope>provided</scope>
		</dependency>
		
	</dependencies>

	<properties>
		<!--  local copy of hpweb-core.jar is packaged in car, and not
				the same version as defined version.  -->
		<cleansheet-core-version>1.0.0-SNAPSHOT</cleansheet-core-version>
		<cleansheet-resources-version>1.0.0-SNAPSHOT</cleansheet-resources-version>
	</properties>
	
	<build>
      
    <plugins>
		<!-- The following two plugins repackage cleansheet-core.jar in order to work around the WebLogic
		bug related to *.tag files embedded in a *.jar file which get recompiled on every request.
		The JAR file is exploded into WEB-INF directory -->
		<plugin>
			<groupId>org.apache.maven.plugins</groupId>
			<artifactId>maven-dependency-plugin</artifactId>
			<executions>
				<execution>
					<id>unpack-dependecines-tags</id>
					<phase>generate-resources</phase>
					<goals>
						<goal>unpack-dependencies</goal>
					</goals>
					<configuration>
						<includeArtifactIds>cleansheet-core</includeArtifactIds>
						<excludeTransitive>true</excludeTransitive>
						<includes>
							**/*.*
						</includes>
						<outputDirectory>
							${project.build.directory}/cleansheet-core/
             				</outputDirectory>
					</configuration>
				</execution>
			</executions>
		</plugin>
		
		<plugin>
			<artifactId>maven-antrun-plugin</artifactId>
			<version>1.6</version>
			<executions>
				<execution>
					<phase>generate-resources</phase>
					<configuration>
						<target>
							<copy todir="${project.build.directory}/${project.build.finalName}/WEB-INF/classes/com">
								<fileset dir="${project.build.directory}/cleansheet-core/com" />
							</copy>
							<copy todir="${project.build.directory}/${project.build.finalName}/WEB-INF/tags/cleansheet">
								<fileset dir="${project.build.directory}/cleansheet-core/META-INF/tags" />
								<fileset dir="${project.build.directory}/cleansheet-core/META-INF" includes="**/*.tld" />
							</copy>
							<replace dir="${project.build.directory}/${project.build.finalName}/WEB-INF/tags/cleansheet" 
									includes="*.tld" token="/META-INF/tags/" value="/WEB-INF/tags/cleansheet/" />
							<jar destfile="${project.build.directory}/${project.build.finalName}/WEB-INF/lib/cleansheet-core-repackaged.jar" 
									basedir="${project.build.directory}/cleansheet-core" includes="META-INF/com/**,META-INF/maven/**" />
						</target>
					</configuration>
					<goals>
						<goal>run</goal>
					</goals>
				</execution>
			</executions>
		</plugin>

	  <!--  Document the jar versions in the manifest file since the
			jars filenames don't include the version numbers.  -->

		<plugin>
    		<groupId>com.hp.it.spf.tools</groupId>
    		<artifactId>spf-maven-car-plugin</artifactId>
    		<version>2.1.0</version>
    		<extensions>true</extensions>
    		<configuration>
		        <outputFileNameMapping>@{artifactId}@.@{extension}@</outputFileNameMapping>
          		<archive>
            		<manifestEntries>
              			<hpweb-laf>${project.version}</hpweb-laf>
              			<hpweb-core>${hpweb-core-version}</hpweb-core>
              			<cleansheet-resources>${cleansheet-resources-version}</cleansheet-resources>
            		</manifestEntries>
          		</archive>
    		</configuration>
		</plugin>
		<!-- 
		<plugin>
                  <groupId>org.apache.maven.plugins</groupId>
                  <artifactId>maven-compiler-plugin</artifactId>
                  <version>2.3.2</version>
                  <configuration>
                        <source>1.5</source>
                        <target>1.5</target>
                  </configuration>  
        </plugin>
		-->
		<plugin>
         	<artifactId>maven-war-plugin</artifactId>
         	<version>2.0</version>
         	<configuration>
           	<archiveClasses>true</archiveClasses>
         	</configuration>
       </plugin>
       
    </plugins>
  </build>
	
</project>